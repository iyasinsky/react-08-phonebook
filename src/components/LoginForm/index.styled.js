import { styled } from 'styled-components';

export const Form = styled.form`
  margin: 0 auto;
  width: 320px;

  label {
    display: flex;
    flex-direction: column;
    margin-bottom: 16px;
  }
`;
